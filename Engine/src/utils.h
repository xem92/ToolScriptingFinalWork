#pragma once

//#include "linmath.h"
//#include <algorithm>

//template <typename T>
//T clamp(const T& lower, const T& upper, const T& n) {
//    return std::max(lower, std::min(n, upper));
//}
//
//float lerp(float a, float b, float f)
//{
//    return a + f * (b - a);
//}
//
//lm::vec3 lerp(lm::vec3 a, lm::vec3 b, float f)
//{
//    return a.lerp(b, f);
//}